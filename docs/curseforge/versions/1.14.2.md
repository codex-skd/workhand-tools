<h2>v1.14.2 - Config Files Moved to Their Own Folder</h2>

<h3>Change</h3>
<ul>
<li><strong>Config files now live under <code>config/workhand_tools/</code></strong> on both client and server, instead of directly in the shared <code>config/</code> folder. Existing config values are not migrated automatically — if you already customized settings, copy them over to the new file location.</li>
</ul>
